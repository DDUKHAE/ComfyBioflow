# Context Routing Examples

- Transcript-focused quantification request: keep `salmon`.
- Genome-alignment-specific request: consider `STAR` or `featureCounts` as ALT, with an explicit override reason.
- Request for enhanced QC aggregation: add `MultiQC` as ALT, without replacing DESeq2 visualization.

